model:
  name: "MedicalNet3D"
  backbone: "resnet50"
  in_channels: 1
  out_channels: 3
  spatial_dims: 3
  pretrained: true
  checkpoint_path: null
  onnx:
    export: true
    opset_version: 17
    optimize: true
